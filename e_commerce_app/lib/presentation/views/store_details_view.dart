import 'package:flutter/material.dart';

class StoreDetialsView extends StatelessWidget {
  const StoreDetialsView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}